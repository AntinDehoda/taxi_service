{% extends 'base.html.twig' %}

{% block body %}
    <h1>Taxi Order</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" class="btn btn-success">
    {{ form_end(form) }}
{% endblock %}
